spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://local.mysql.com:3306/ycloud-user?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=GMT%2B8
    username: root
    password: root
  rabbitmq:
    host: local.rabbitmq.com
    port: 15672
    username: guest
    password: guest
    publisher-confirms: true
    virtual-host: /
# port
server:
  port: 0
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS